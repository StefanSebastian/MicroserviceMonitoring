# Spring properties
spring:
  application:
    name: microservice3

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: ${PORT:2224}
